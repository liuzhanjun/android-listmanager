<android.support.v4.widget.DrawerLayout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <android.support.v4.view.ViewPager xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/app_background">

        <android.support.v4.view.PagerTitleStrip
            android:id="@+id/pager_title_strip"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="top"
            android:background="@color/pagerBackground"
            android:textColor="@color/pagerText"
            android:paddingTop="@dimen/pager_title_strip_padding_v"
            android:paddingBottom="@dimen/pager_title_strip_padding_v"
            android:paddingLeft="@dimen/pager_title_strip_padding_h"
            android:paddingRight="@dimen/pager_title_strip_padding_h"
            />


        <!-- The main content view -->
        <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:tools="http://schemas.android.com/tools"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingBottom="@dimen/activity_vertical_margin"
            android:paddingLeft="@dimen/activity_horizontal_margin"
            android:paddingRight="@dimen/activity_horizontal_margin"
            android:paddingTop="@dimen/activity_vertical_margin"
            android:background="@android:color/transparent">


        </RelativeLayout>

    </android.support.v4.view.ViewPager>
    <!-- The navigation drawer -->
    <ListView
        android:id="@+id/tagsList"
        android:layout_gravity="start"
        android:layout_width="@dimen/drawer_width"
        android:layout_height="match_parent"
        android:choiceMode="singleChoice"
        android:dividerHeight="@dimen/drawer_divider_height"
        android:divider="@color/drawerDivider"
        android:background="@color/drawerBackground" />

</android.support.v4.widget.DrawerLayout>
   
